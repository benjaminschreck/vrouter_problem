import 'package:flutter/material.dart';

class NoUserPage extends StatelessWidget {
  const NoUserPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('I dont know this user'),
    );
  }
}
